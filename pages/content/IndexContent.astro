---
import { IndexTranslationsInterface } from "../../i18n/interfaces/index.interface";

interface Props {
	translations: IndexTranslationsInterface;
}

const { translations } = Astro.props;
const t = translations;
---
